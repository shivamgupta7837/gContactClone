import 'package:flutter/material.dart';

class ContactDetails extends StatelessWidget {
  String? name;
   ContactDetails({this.name}) ;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}